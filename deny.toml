# Configuration cargo-deny pour OBSI (workspace build/)
# Vérifiée par le job "Security Audit" du workflow CI.

[graph]
# Analyse le workspace complet
exclude = []

[licenses]
# Politique (cargo-deny ≥ 0.14, PR #611) : tout ce qui n'est PAS dans `allow`
# est refusé par défaut — `deny`, `unlicensed`, `allow-osi-fsf-free`, `default`
# ont été retirés du format.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "0BSD",
    "ISC",
    "MPL-2.0",
    "Unicode-3.0",
    "Zlib",
    "OpenSSL",
    "BSL-1.0",
    "CDLA-Permissive-2.0",
]

# Exceptions documentées si nécessaire (aucune pour l'instant)
exceptions = [
    # { name = "exemple-crate", version = "*", allow = ["License-Exception"] },
]

[advisories]
# Les vulnérabilités connues (rustsec) font échouer le CI
yanked = "deny"
ignore = [
    # RUSTSEC-2024-0413 + 0416 : bindings GTK3 "unmaintained" — dépendance
    # transitive OBLIGATOIRE de Tauri v2 (webkit2gtk-4.1). Pas de remplacement
    # possible sans changer toute la stack GUI. Aucune vulnérabilité active.
    "RUSTSEC-2024-0413",
    "RUSTSEC-2024-0416",
    # RUSTSEC-2025-0012 : crate `backoff` non maintenu — dépendance transitive
    # d'ollama-rs (retry HTTP). Fonctionnel, pas de vulnérabilité connue.
    "RUSTSEC-2025-0012",
]

[bans]
# Cratas bannis pour raisons de sécurité/maintenance
multiple-versions = "warn"
wildcards = "deny"
deny = [
    # { name = "crate-a-vulnerable", version = "<0.5.0" },
]

[sources]
# Seuls crates.io et git sont autorisés (pas de registres inconnus)
unknown-registry = "deny"
unknown-git = "deny"
allow-git = []

[output]
# Format lisible pour les logs CI
feature-depth = 1
